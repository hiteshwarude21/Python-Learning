name = "Hello"

print(name[0:3])

print(name[-4: -1])
print(name[1:4])
print(name[:5])  # is sam as [0:5] because the default start index is 0
print(name[1:])  # is same as [1:5] because the default end index is the length of the string
print(name[1:5])
